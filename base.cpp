#include "funciones.h"
#include "base.h"

map<string, map<string, int> > base(map<string, map<string, int> > inicial)
{
    map <string,map<string,int>> final;
    cout << "\n\n\n YA CASI \n\n\n";













    return final;
}
